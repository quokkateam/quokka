sudo: false

node_js:
  - 6

install:
  - gem install sass
  - mkdir -p src/styles/css

cache:
  directories:
    - node_modules

script:
  - npm run sass:build
  - npm test -- --coverage
  - npm run build

after_success:
  - codecov
